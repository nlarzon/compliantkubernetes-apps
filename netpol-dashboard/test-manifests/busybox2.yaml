apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: busybox2
  name: busybox2
spec:
  replicas: 4
  selector:
    matchLabels:
      app: busybox2
  template:
    metadata:
      labels:
        app: busybox2
    spec:
      containers:
      - image: busybox
        name: busybox2
        command: ['sh', '-c', 'while true; do wget ingress-nginx-defaultbackend.ingress-nginx.svc.cluster.local; sleep 1; done;']
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: busybox2-privileged
  namespace: default
rules:
- apiGroups:
  - extensions
  resourceNames:
  - privileged
  resources:
  - podsecuritypolicies
  verbs:
  - use
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: busybox2-privileged
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: busybox2-privileged
subjects:
- kind: ServiceAccount
  name: default
  namespace: default
---
